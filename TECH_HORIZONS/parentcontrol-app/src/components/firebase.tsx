import { initializeApp } from "firebase/app";


export class AppComponent {
    constructor() {
        this.initializeApp();
    }

    initializeApp() {
        //const firebaseApp = initializeApp(environment.firebase);
        // Now you can use firebaseApp for Firebase initialization
    }
}
