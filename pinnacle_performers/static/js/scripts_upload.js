function selectSignatureType(type) {
    // Here you can define what happens when a signature type is selected
    alert('Selected signature type: ' + type);
    // You might want to redirect the user or update the UI accordingly
  }
  
